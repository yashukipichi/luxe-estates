{
  "name": "luxe-estates",
  "version": "1.0.0",
  "description": "Luxury real estate platform — Hyderabad",
  "main": "server.js",
  "scripts": {
    "start":  "node server.js",
    "dev":    "nodemon server.js",
    "seed":   "curl -s -X POST http://localhost:5000/api/seed/properties | node -e \"process.stdin.resume();let d='';process.stdin.on('data',c=>d+=c).on('end',()=>console.log(JSON.parse(d).message))\""
  },
  "dependencies": {
    "cors":       "^2.8.5",
    "dotenv":     "^16.3.1",
    "express":    "^4.18.2",
    "mongoose":   "^7.6.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
